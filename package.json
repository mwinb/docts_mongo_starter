{
  "name": "ts_express_mongo_starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "export NODE_ENV=development && nodemon ./src/server.ts",
    "prod": "export NODE_ENV=production && npm run build && node ./dist/server.js",
    "start:dev": "./start-mongo.sh || .\\start-mongo.sh && npm run start",
    "test": "jest --watchAll",
    "test:unit": "jest --watchAll=false --silent --coverage --bail"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/jest": "^26.0.20",
    "@types/node": "^14.14.20",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.11.11",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1"
  }
}
